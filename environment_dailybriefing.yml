name: google_agent_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.14
  - pip
  - pip:
      - caldav
      - vobject
      - ipykernel
      - python-dateutil
      - google-api-python-client
      - google-auth-oauthlib
      - google-auth
      - google-genai  # This is the package for 'from google import genai'
      - openpyxl  # For reading Patterson_Inventory.xlsx liquor pricing